# Discord webhook URL (required for notifications)
CLAUDE_NOTIFY_WEBHOOK=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# Bot name (optional, defaults to "Claude Code")
CLAUDE_NOTIFY_BOT_NAME=Claude Code

# Enable automatic cleanup of old notifications (recommended)
# When enabled, old messages are deleted when new ones are posted for the same project+event
# Prevents channel spam from repeated notifications
CLAUDE_NOTIFY_CLEANUP_OLD=true

# Permission prompt color (optional, defaults to orange)
# Orange (#FFA500) makes permission prompts stand out as urgent actions
# Set to another color decimal value to customize (see README for color table)
# CLAUDE_NOTIFY_PERMISSION_COLOR=16753920

# Approval color (optional, defaults to green)
# Green (#2ECC71) indicates permission was granted and tool executed successfully
# Permission messages automatically update to this color when approved
# CLAUDE_NOTIFY_APPROVAL_COLOR=3066993

# Session lifecycle colors (optional)
# Green (#2ECC71) for session start - shows when Claude Code session begins
# CLAUDE_NOTIFY_ONLINE_COLOR=3066993

# Red (#E74C3C) for session end - updates last message when session terminates
# CLAUDE_NOTIFY_OFFLINE_COLOR=15158332

# Discord bot token (optional, only needed for bulk operations like channel cleanup)
# This is separate from the webhook - get it from Discord Developer Portal
# To use: DISCORD_BOT_TOKEN=<token> bash scripts/discord-bulk-delete.sh <channel_id>
# DISCORD_BOT_TOKEN=YOUR_BOT_TOKEN_HERE

# Discord bulk delete rate limit (optional, defaults to 0.5 seconds)
# Delay between message deletions to avoid Discord rate limiting
# Decrease for faster deletion (risk rate limit), increase for more conservative
# DISCORD_DELETE_DELAY=0.5

# Approval message TTL (optional, defaults to 1 hour)
# How long to keep approved permission messages before cleanup
# Set to 0 to disable automatic cleanup, increase to keep audit history longer
# CLAUDE_NOTIFY_APPROVAL_TTL=1

# Show additional context in notifications (optional, defaults to false)
# Session info: session ID (shortened) and permission mode
# CLAUDE_NOTIFY_SHOW_SESSION_INFO=false

# Show tool details in permission notifications (tool name, command)
# CLAUDE_NOTIFY_SHOW_TOOL_INFO=false

# Show full working directory path instead of just project name
# CLAUDE_NOTIFY_SHOW_FULL_PATH=false
